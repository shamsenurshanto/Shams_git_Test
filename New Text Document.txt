git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/shamsenurshanto/Shams_git_Test.git
git push -u origin main


***************** update ***********


now we will do the branching 


git branch my-feature-branch

git checkout my-feature-branch
  
  git push --set-upstream origin my-feature-branch

********************** merging the two branches *********

git checkout master
git merge my-feature-branch  // master 23 no line diye gesi pore 24 no line diye ei branher code oi main e disi 

git push // na korle local ei sob thaikka jabe

clone it using 
git clone [copied link ]


************************
just normally git clone kore oi file ei shundr kore vs code dia push likhlei hoy just mone rakhte hobe path ta thik ase naki 

******** ekhn update data kmne nibo

git fecth 
git pull
